# MOP (MesOpsPlatform) 项目说明文档

## 一、 核心架构机制

### 1.1 登录认证流程 (Authentication Flow)

系统采用基于 **JWT (JSON Web Token)** 的无状态认证机制，适配前后端分离架构。

1. **验证码阶段**: 前端请求 `/captchaImage`；后端通过 `kaptcha` 生成图片并将 `uuid` 与文本映射存入 **Redis**。
2. **登录提交**: 前端 POST 用户信息、验证码及 `uuid` 到 `/login`。
3. **后端认证**:
    * **验证码校验**: `SysLoginController` 从 Redis 核对验证码。
    * **身份认证**: `AuthenticationManager` 委托 `UserDetailsServiceImpl` 加载用户信息并完成 **BCrypt** 密码比对。
    * **Token生成**: 认证成功后，`TokenService` 生成 JWT，并将用户信息（角色、权限等）存入 **Redis**。
4. **后续鉴权**: 前端请求头携带 `Authorization: Bearer <token>`。后端 `JwtAuthenticationTokenFilter` 拦截并解析 Token，从
   Redis 获取信息并注入 `SecurityContextHolder`。

### 1.2 权限系统设计 (Authorization Model)

系统实现了**菜单/按钮权限**和**数据权限**的双重控制。

#### A. 核心模型：RBAC (Role-Based Access Control)

- **用户 -> 角色 -> 权限** 模型。
- 权限表现为精确字符串（如 `system:user:add`），与菜单/按钮绑定。

#### B. 菜单/按钮权限 (API-Level)

- **前端**: 用户登录后获取权限标识列表，通过自定义指令 `v-hasPermi` 控制 UI 元素显隐。
- **后端**: 使用 `@PreAuthorize("@ss.hasPermi('...')")` 拦截。`PermissionService (@ss)` 检查当前用户的权限集合。

#### C. 数据权限 (Row-Level)

通过 **AOP** 实现对业务代码无侵入的行级过滤。

1. **`@DataScope`**: 标记 Service 层查询方法。
2. **`DataScopeAspect` 切面**:
    * **拦截**: 在方法执行前获取用户角色及对应数据范围。
    * **拼接**: 动态生成符合 SQL Server 语法的 `WHERE` 子句（注意 SQL Server 对 IN 子句参数数量的限制及分页偏移逻辑）。
    * **注入**: 将 SQL 片段存入 `BaseEntity` 的 `params` 属性。
    * **执行**: MyBatis 在执行时应用该动态条件。

---

## 二、 全局通用组件与策略

### 2.1 全局异常处理 (Global Exception Handling)

* **代码位置**: `framework.web.exception.GlobalExceptionHandler.java`
* **实现**: 基于 `@RestControllerAdvice`。
* **价值**: 统一捕获 `ServiceException`、`AccessDeniedException` 等，向前端返回标准化 `AjaxResult` JSON 响应，避免泄露堆栈信息。

### 2.2 异步任务与线程池 (Asynchronous Execution)

* **代码位置**: `framework.manager.AsyncManager`, `framework.config.ThreadPoolConfig`
* **实现**: 通过 `@EnableAsync` 开启，自定义 `threadPoolTaskExecutor` 精细化配置线程池参数。
* **使用**: 在非核心路径（如日志记录、通知发送）使用 `@Async`，提升主线程响应速度。

### 2.3 接口防刷与限流 (Rate Limiting)

* **代码位置**: `framework.aspectj.RateLimiterAspect.java`
* **实现**: 自定义 `@RateLimiter` 注解。
* **逻辑**: 基于 **Redis + Lua 脚本**实现令牌桶算法，在 AOP 层面对高频请求进行拦截，保障系统稳定性。

### 2.4 防止重复提交 (Repeat Submission Prevention)

* **代码位置**: `framework.interceptor.RepeatSubmitInterceptor.java`
* **实现**: 基于 Spring MVC 拦截器及 `@RepeatSubmit` 注解。
* **逻辑**: 根据 URL + 参数 + Token 生成 Redis 唯一 Key 锁（如3秒），防止网络延迟导致的业务数据重复创建。

### 2.5 多数据源与读写分离 (Dynamic Data Source)

* **代码位置**: `framework.aspectj.DataSourceAspect.java`, `framework.datasource.DynamicDataSource.java`
* **实现**: 自定义 `@DataSource` 注解配合 AOP。
* **逻辑**: 通过 `ThreadLocal` 存储数据源标识，由 `DynamicDataSource` 动态路由至主库 (MASTER) 或从库 (SLAVE)，提升高并发场景吞吐量。

---

## 三、 系统管理 (System Management)

### 3.1 用户管理

* **目标**: 管理登录账户，是权限体系的主体。
* **核心逻辑**:
    * **前端**: `views/system/user/index.vue`。集成部门树筛选。
    * **后端**: `SysUserController.java`。新增/修改用户时通过 `SecurityUtils.encryptPassword()` 进行 BCrypt
      加密，并维护多岗位、多角色关联。

### 3.2 角色管理

* **目标**: RBAC 核心，绑定菜单权限与数据权限。
* **功能**: 为角色分配**菜单权限**（树形勾选）及**数据范围**（全部、本部门、自定义等）。修改后同步清理 Redis 缓存以生效。

### 3.3 菜单管理

* **目标**: 定义导航结构。
* **资源分类**: 目录 (M)、菜单 (C)、按钮 (F)。
* **核心接口**: `getRouters()` 接口根据权限动态构建树形路由，前端 `Vue Router` 动态渲染。

### 3.4 部门与岗位管理

* **部门**: 支持树形层级，利用 `ancestors` 字段（祖先路径）优化递归查询性能。
* **岗位**: 维护固定职位（如“工程师”），用于流程标识。

### 3.5 字典与参数设置

* **字典**: 管理固定键值对（如性别、状态）。通过 **Redis 缓存**提升读取效率。
* **参数**: 管理动态系统配置（如验证码开关），无需重启服务即可生效。

### 3.6 日志管理

* **操作日志**: 基于 `@Log` 注解与 AOP，**异步**记录用户请求参数、返回值及异常。
* **登录日志**: 记录用户登录 IP、地点、浏览器及状态。

---

## 四、 系统监控 (System Monitoring)

### 4.1 在线用户

* **定义**: 基于 Redis 存储的会话。
* **强退**: 通过从 Redis 中删除指定 Token 键值实现。

### 4.2 定时任务

* **框架**: 深度集成 **Quartz**。
* **逻辑**: 支持 Cron 表达式。通过 **Java 反射**机制动态执行业务 Bean 的方法。

### 4.3 基础设施监控

* **数据监控**: 基于 **Alibaba Druid** 控制台，提供 SQL 耗时与连接池分析。
* **服务监控**: 利用 `oshi-core` 库实时获取 CPU、内存、JVM、磁盘状态。
* **缓存监控**: 提供可视化界面管理 Redis 键空间与服务器状态。

---

## 五、 系统工具 (System Tools)

### 5.1 代码生成 (Code Gen)

* **框架**: **Apache Velocity** 模板引擎。
* **流程**: 查询 `information_schema` 获取元数据，根据配置渲染 `.vm` 模板，一键生成全栈 CRUD 代码。

### 5.2 表单构建

* 可视化拖拽式表单设计，自动生成 JSON Schema，加速前端开发。

### 5.3 系统接口

* **框架**: **SpringDoc** (OpenAPI 3)。
* **功能**: 自动扫描注解生成交互式文档，支持在线调试 (Try it out)，替代传统手写文档。

---

## 第一部分：系统管理 (System Management)

“系统管理”是整个平台的控制中枢与数据基石。它包含了维护系统正常运转所需的所有基础数据和权限配置功能。

### 1.1. 用户管理

* **业务目标：**
  管理所有能够登录和使用本系统的实体账户。此模块是整个权限体系（Authentication & Authorization）的入口和主体。

* **核心功能点：**
    * **查询/检索：** 提供按登录名称、手机号码、用户状态、创建时间范围等多维度组合查询，并以分页列表形式展示。支持对查询结果进行导出为Excel。
    * **新增用户：** 创建新用户账户，必填项包括归属部门、登录名称、用户昵称和密码。系统会校验登录名称的唯一性。可同时关联一个或多个岗位。
    * **修改用户：** 更新用户的基本信息（昵称、邮箱、手机）、归属部门、岗位。提供“重置密码”功能，可将密码重置为系统默认或随机生成的密码。
    * **删除用户：** 支持批量逻辑删除用户。为防止误操作，系统默认执行逻辑删除（修改用户状态或删除标记），而非物理删除。
    * **状态控制：** 可对用户进行“启用”或“停用”操作。被停用的用户在认证环节将被拒绝登录。
    * **分配角色：** 核心功能。以弹窗形式列出所有可用角色，管理员可为单个用户勾选一个或多个角色。用户的最终权限是其所有角色的权限总和。

* **技术实现逻辑：**
    * **前端：**
        * **视图：** `views/system/user/index.vue`
        * **交互：** 使用 Element UI 的 `el-table`、`el-pagination`、`el-form`、`el-dialog` 等组件构建。左侧会显示部门树（
          `el-tree`），点击部门可按部门筛选用户。
        * **API调用：** 所有与后端的交互都通过 `api/system/user.js` 中封装的 `Axios` 请求函数完成。
    * **后端：**
        * **控制器：** `SysUserController.java`。使用 `@PreAuthorize` 注解在方法级别进行严格的操作权限控制（如
          `hasPermi('system:user:add')`）。
        * **服务层：** `SysUserServiceImpl.java`。处理核心业务逻辑，如：新增用户时对密码进行BCrypt加密 (
          `SecurityUtils.encryptPassword()`)
          ；校验用户名、手机号的唯一性；在新增或修改用户时，处理其与角色、岗位关联表的更新 (`sys_user_role`, `sys_user_post`)
          ，此过程由 `@Transactional` 注解保证事务性。
        * **数据权限：** `selectUserList` 方法上应用了 `@DataScope` 注解，`DataScopeAspect`
          切面会根据当前操作员的角色，自动在SQL查询中注入数据范围过滤条件，实现行级别安全。
    * **数据模型：** 主表为 `sys_user`（使用 `IDENTITY` 自增种子），通过 `dept_id` 关联 `sys_dept` 表，通过中间表
      `sys_user_role` 和
      `sys_user_post` 实现与角色和岗位的多对多关联。

### 1.2. 角色管理

* **业务目标：**
  创建和管理“角色”这一抽象概念，并为角色绑定具体的“权限”。它是实现RBAC（基于角色的访问控制）模型的核心。

* **核心功能点：**
    * **查询/检索：** 按角色名称、权限字符、状态、创建时间等进行查询和分页展示。
    * **新增/修改角色：** 定义角色的基本信息，如角色名称、权限字符串（`roleKey`，如`admin`、`common`，用于后端代码判断）、显示顺序等。
    * **权限分配：** 这是本模块最核心的功能。
        * **菜单权限：** 以树形结构（`el-tree`）展示所有系统菜单（包括目录、菜单、按钮）。管理员通过勾选，为角色精确分配其可见的菜单和可操作的按钮。
        * **数据权限：** 为角色配置其能看到的数据范围，包括：全部数据、自定义数据（可勾选特定部门）、本部门数据、本部门及以下数据、仅本人数据。
    * **分配用户：** 提供一个反向操作的便捷入口，可以查看已分配给某角色的所有用户，并进行批量添加或移除。

* **技术实现逻辑：**
    * **前端：**
        * **视图：** `views/system/role/index.vue`
        * **交互：** 权限分配的弹窗是交互核心。菜单树使用 `el-tree` 组件，通过 `show-checkbox` 属性实现勾选。保存时，调用
          `getCheckedKeys()` 和 `getHalfCheckedKeys()` 获取全选和半选的节点ID，一并提交。
    * **后端：**
        * **控制器：** `SysRoleController.java`。
        * **服务层：** `SysRoleServiceImpl.java`。处理权限分配的 `authDataScope` 或类似方法时，会执行“先删除后插入”的逻辑来更新
          `sys_role_menu` 关联表，并使用 `@Transactional` 保证原子性。修改角色权限后，会清空相关的Redis缓存，以确保拥有该角色的用户能立即加载到新权限。
        * **数据模型：** 主表 `sys_role`。通过 `sys_role_menu` 关联菜单，通过 `sys_role_dept` 关联自定义数据权限的部门，通过
          `sys_user_role` 关联用户。

### 1.3. 菜单管理

* **业务目标：**
  定义和管理系统的导航结构及所有可操作的权限点。它不仅是前端界面的“骨架”，更是后端权限校验的“原子单位”。

* **核心功能点：**
    * **树形管理：** 以树形表格的形式对所有“资源”进行层级化管理。
    * **三类资源：**
        * **目录(M):** 作为导航栏的一级或二级菜单，用于组织下级，本身不可点击跳转。
        * **菜单(C):** 可点击的导航项，关联一个前端路由和页面组件。
        * **按钮(F):** 页面内部的具体操作权限，如“新增”、“删除”按钮。它在导航栏中不可见，但其定义的“权限标识”是后端进行精细化权限控制的依据。
    * **属性配置：** 可为每个菜单/按钮配置其上级、图标、名称、路由地址、组件路径、可见/隐藏状态以及最重要的**权限标识**（如
      `system:user:add`）。

* **技术实现逻辑：**
    * **前端：**
        * **视图：** `views/system/menu/index.vue`。使用支持树形数据的表格组件。
        * **交互：** 新增/修改表单会根据选择的“资源类型”动态显示/隐藏相关输入项。
    * **后端：**
        * **控制器：** `SysMenuController.java`。
        * **核心接口 `getRouters()`：** 用户登录后，前端会调用此接口。后端根据用户ID，通过`JOIN`查询 `sys_user_role`,
          `sys_role_menu`, `sys_menu` 三张表，筛选出该用户有权访问的所有“菜单”和“目录”类型的资源，构建成树形结构返回。前端
          `Vue Router` 根据此数据动态生成路由表和侧边栏导航。
        * **服务层：** `SysMenuServiceImpl.java`。包含将扁平的菜单列表构建成树形结构的业务逻辑。
        * **数据模型：** `sys_menu` 表。通过 `parent_id` 字段自关联形成树形结构。`perms` 字段存储的权限标识是整个权限系统的关键连接点。

### 1.4. 部门管理

* **业务目标：**
  维护企业或组织的层级结构。它是数据权限和组织架构的基础。

* **核心功能点：**
    * 以树形结构展示、新增、修改、删除部门。
    * 可以设置部门负责人、联系方式等信息。
    * 可以控制部门的状态（正常/停用）。

* **技术实现逻辑：**
    * **实现模式：** 与菜单管理高度相似，都是对树形结构数据的CRUD操作。
    * **数据权限的基石：** `sys_dept` 表是 `DataScopeAspect` 实现“按部门”进行数据过滤的基础。
    * **数据模型：** `sys_dept` 表。除了 `parent_id`，通常会有一个 `ancestors`
      字段，用逗号分隔存储从根到当前节点的所有祖先ID路径。这个设计是一个重要的性能优化，它使得查询一个部门的所有子孙部门时，可以简单地使用
      `WHERE ancestors LIKE '%,deptId,%'`（在 SQL Server 中需注意 Collation 对 LIKE 查询性能的影响），而无需耗费资源的递归查询。

### 1.5. 岗位管理

* **业务目标：**
  管理组织内的固定职位或岗位，如“项目经理”、“软件工程师”等。它通常用于流程审批或信息标识，权限控制的能力弱于角色。

* **核心功能点：**
    * 对岗位进行增、删、改、查。
    * 可设置岗位编码、名称、显示顺序和状态。

* **技术实现逻辑：**
    * **实现模式：** 一个标准的、无层级关系的CRUD模块。
    * **控制器：** `SysPostController.java`。
    * **数据模型：** `sys_post` 表存储岗位信息。`sys_user_post` 作为中间表，记录用户和岗位的多对多关系。

### 1.6. 字典管理

* **业务目标：**
  对系统中需要复用的、固定或不常变动的“键值对”数据进行统一管理，以避免在代码中硬编码。例如：用户性别（0=男, 1=女,
  2=未知）、任务状态（0=进行中, 1=已暂停）等。

* **核心功能点：**
    * **字典类型管理：** 创建字典的大类，如“用户性别(`sys_user_sex`)”。
    * **字典数据管理：** 在某个字典类型下，维护具体的键值对数据，如“男/0”、“女/1”。

* **技术实现逻辑：**
    * **前端：** 框架提供了全局的 `getDicts` 方法，可以在任何Vue组件中方便地异步获取字典数据，并用于渲染下拉框、单选按钮组等。
    * **后端：**
        * **控制器：** `SysDictTypeController` 和 `SysDictDataController`。
        * **性能优化：** 字典数据是典型的“读多写少”数据。系统在启动时或首次访问时，会将所有字典数据加载到 **Redis 缓存**
          中。后续所有对字典的请求都直接从Redis获取，性能极高。当后台修改字典数据后，会同步更新或清空Redis中的缓存。
        * **工具类：** `DictUtils.java` 提供了从缓存中方便地获取字典标签或值的静态方法。
    * **数据模型：** `sys_dict_type` (字典类型表) 和 `sys_dict_data` (字典数据表)。

### 1.7. 参数设置

* **业务目标：**
  对系统级的、需要在运行时动态调整的配置参数进行管理，而无需修改代码或重启服务。

* **核心功能点：**
    * **参数维护：** 对参数的“键”（Key）、“值”（Value）进行增、删、改、查。
    * **内置参数：** 可设置参数是否为系统内置。内置参数通常不允许前台删除。

* **技术实现逻辑：**
    * **实现模式：** 与字典管理非常相似，也是一个键值对管理系统。
    * **控制器：** `SysConfigController.java`。
    * **性能优化：** 同样地，所有系统参数也被缓存在 **Redis** 中，以提供快速的读取性能。修改参数后会同步刷新缓存。
    * **数据模型：** `sys_config` 表。

### 1.8. 通知公告

* **业务目标：**
  提供一个简单的内容发布功能，用于向系统所有用户发布通知或公告。

* **核心功能点：**
    * **公告维护：** 对公告进行增、删、改、查。
    * **内容编辑：** 通常会集成一个富文本编辑器（如 `Editor` 组件），支持图文混排。

* **技术实现逻辑：**
    * **实现模式：** 一个标准的内容管理（CMS）型CRUD模块。
    * **控制器：** `SysNoticeController.java`。
    * **数据模型：** `sys_notice` 表。

### 1.9. 日志管理

这是一个目录，包含了两种不同类型的日志。

#### 1.9.1. 操作日志

* **业务目标：**
  记录用户在系统中的所有关键操作，用于事后审计、问题追溯和行为分析。满足“谁，在什么时间，从哪里，做了什么，结果如何”的审计需求。

* **核心功能点：**
    * **查询/检索：** 可按操作模块、操作人员、操作类型、状态（成功/失败）、时间范围等进行查询。
    * **详情查看：** 可查看每次操作的详细信息，包括请求的URL、方法、参数、返回值和错误信息。

* **技术实现逻辑：**
    * **数据来源：** 日志数据并非在业务代码中手动记录，而是通过 **AOP (面向切面编程)** 自动捕获。
    * **实现核心：** `LogAspect.java` 切面类。它拦截所有被 `@Log`
      注解标记的Controller方法。在方法执行后，它通过Java反射和Servlet工具类收集操作的全部上下文信息，然后**异步地**
      （通过线程池）将这些信息封装成 `SysOperLog` 对象，并存入数据库。
    * **控制器：** `SysOperlogController.java` 负责从 `sys_operlog` 表中读取数据并展示。

#### 1.9.2. 登录日志

* **业务目标：**
  专门记录用户的登录行为，包括成功登录和失败尝试，用于安全监控和用户行为分析。

* **核心功能点：**
    * **查询/检索：** 可按登录地址、登录状态、用户名、时间范围等进行查询。
    * **信息展示：** 列表展示登录账号、IP地址、登录地点、浏览器、操作系统、状态（成功/失败）和操作信息。

* **技术实现逻辑：**
    * **数据来源：** 在用户登录流程中，`SysLoginService` 或一个专门的事件监听器（`AuthenticationSuccessEvent` /
      `AuthenticationFailureBadCredentialsEvent`）会捕获登录结果。
    * **信息记录：** 捕获到登录事件后，系统会创建一个 `SysLogininfor` 对象，记录下所有相关信息，并**异步地**将其存入数据库。
    * **控制器：** `SysLogininforController.java` 负责从 `sys_logininfor` 表中读取数据并展示。

---

## 第二部分：系统监控 (System Monitoring)

“系统监控”提供了一系列用于观察和管理系统自身运行状态的工具，是系统稳定运行和问题排查的重要保障。

### 2.1. 在线用户

* **业务目标：**
  实时查看当前“活跃”在系统中的用户，并为管理员提供对这些用户会话的管理能力。

* **核心功能点：**
    * **列表展示：** 显示当前在线用户的会话ID、登录名称、IP地址、登录地点、浏览器和登录时间。
    * **强制退出：** 管理员可以选中一个或多个在线用户，强制其下线。被强退的用户在下一次操作时会被重定向到登录页。

* **技术实现逻辑：**
    * **“在线”的定义：** 本系统的“在线”并非基于传统的HTTP Session。用户登录成功后，其身份信息（`LoginUser`）会被缓存到 *
      *Redis** 中，并设置一个过期时间（例如30分钟）。只要这个Redis缓存存在，用户就被认为是“在线”的。用户的每个操作都会刷新这个缓存的过期时间。
    * **数据来源：** `SysUserOnlineController` 通过扫描Redis中所有符合特定前缀（如 `login_tokens:*`）的键（Key）来获取在线用户列表。
    * **强退实现：** “强制退出”操作的本质就是**从Redis中删除指定的会话键**。当用户的下一次请求到达时，
      `JwtAuthenticationTokenFilter` 会因为在Redis中找不到对应的会话信息而认证失败，从而实现强退。

### 2.2. 定时任务

* **业务目标：**
  提供一个可视化的界面，用于管理和调度需要在特定时间或周期性执行的后台任务，而无需修改代码或重启服务。

* **核心功能点：**
    * **任务维护：** 对定时任务进行增、删、改、查。
    * **核心配置：**
        * **调用目标：** 指定要执行的方法，格式通常为 `beanName.methodName(params)`，例如 `ryTask.ryParams('hello')`。
        * **Cron表达式：** 使用标准的Cron表达式定义任务的执行周期。
    * **任务控制：** 可对任务进行“执行一次”、“暂停”、“恢复”、“启动”等操作。
    * **日志查看：** 查看每个任务的历史执行记录、执行结果和耗时。

* **技术实现逻辑：**
    * **核心框架：** 深度集成了 **Quartz** 开源任务调度框架。
    * **控制器：** `SysJobController.java`。
    * **交互核心：** `ScheduleUtils.java` 工具类。当用户在界面上操作任务时，`SysJobController` 会调用 `ScheduleUtils`
      中相应的方法。`ScheduleUtils` 负责将数据库中的任务信息（`sys_job` 表）翻译成Quartz能够理解的 `JobDetail` 和 `Trigger`
      对象，并调用Quartz的API进行任务的创建、暂停、删除等操作。
    * **任务执行：** Quartz在指定时间触发任务时，会执行一个通用的 `QuartzJobExecution` 类。这个类会根据数据库中配置的“调用目标字符串”，通过
      **Java反射** 机制动态地找到并执行真正的业务方法。这种设计实现了调度与执行的解耦。
    * **数据模型：** `sys_job` (任务信息表) 和 `sys_job_log` (任务执行日志表)。

### 2.3. 数据监控

* **业务目标：**
  为数据库管理员和开发人员提供一个强大的、可视化的数据库连接池监控工具。

* **核心功能点：**
    * **SQL监控：** 监控所有执行过的SQL语句，包括执行耗时、执行次数、结果集大小等。
    * **URL监控：** 监控每个URL请求对应的数据库访问情况。
    * **连接池监控：** 实时查看连接池的各项参数，如活动连接数、空闲连接数、创建/销毁次数等。

* **技术实现逻辑：**
    * **核心框架：** 完全基于 **Alibaba Druid** 数据库连接池自带的监控功能。
    * **实现方式：** 系统通过 `DruidConfig.java` 配置文件，启用并暴露了Druid的内置监控Servlet (`StatViewServlet`)
      。前端“数据监控”页面实际上是一个 `iframe`，其 `src` 指向的就是这个Servlet的地址（通常是 `/druid/index.html`
      ）。所有展示的数据和图表都由Druid自身提供。

### 2.4. 服务监控

* **业务目标：**
  提供对应用服务器自身硬件及JVM（Java虚拟机）状态的实时监控。

* **核心功能点：**
    * **CPU信息：** 显示CPU核心数、系统使用率、用户使用率、当前空闲率等。
    * **内存信息：** 显示服务器总内存、已用内存、剩余内存。
    * **JVM信息：** 显示JVM总内存、已用内存、可用内存、启动时间、运行时长等。
    * **磁盘状态：** 列出服务器所有磁盘分区的总空间、已用空间和可用空间。

* **技术实现逻辑：**
    * **核心库：** `oshi-core`。这是一个跨平台的Java库，专门用于获取底层的操作系统和硬件信息。
    * **控制器：** `ServerController.java`。该控制器不进行任何计算，它只是创建一个 `Server` 实体对象，该对象在内部调用
      `oshi-core` 的API来填充所有数据，然后控制器将该对象返回给前端。

### 2.5. 缓存监控

* **业务目标：**
  提供一个基础的、可视化的界面来查看和管理Redis缓存中的数据。

* **核心功能点：**
    * **缓存列表：** 展示Redis中所有的缓存键（Key）。
    * **缓存详情：** 查看指定键的详细信息，包括类型、过期时间、和存储的值。
    * **清理缓存：** 提供一键清除所有缓存或删除指定缓存的功能。

* **技术实现逻辑：**
    * **控制器：** `CacheController.java`。

---

## 第三部分：全局通用组件与策略

除了具体的业务功能模块，MOP 系统还构建了一系列贯穿全局的、用于提升系统健壮性、安全性和性能的通用组件与策略。这些是系统“内功”的体现。

### 3.1. 全局异常处理 (Global Exception Handling)

* **代码位置:** `framework.web.exception.GlobalExceptionHandler.java`
* **目标与价值:** 确保任何情况下后端都能向前端返回统一、规范的JSON错误响应。它通过 `@RestControllerAdvice`
  注解，集中捕获并处理应用中所有未被捕获的异常，如业务逻辑异常 (`ServiceException`)、权限校验异常 (`AccessDeniedException`)
  、参数校验失败 (`BindException`) 等。这避免了在每个控制器中编写重复的`try-catch`逻辑，极大地提升了代码的整洁度和用户体验的一致性。

### 3.2. 异步任务与线程池 (Asynchronous Execution)

* **代码位置:** `framework.manager.AsyncManager`, `framework.config.ThreadPoolConfig`
* **目标与价值:** 提升系统核心业务流程的响应速度和吞吐量。对于那些非核心、但可能耗时的操作（如记录操作日志、发送通知），系统通过
  `AsyncManager.execute()` 方法将其提交到一个自定义的后台线程池中异步执行。这意味着主线程可以立即完成并返回响应给用户，而无需等待这些辅助操作完成，从而显著改善了用户体验。

### 3.3. 接口防刷与限流 (Rate Limiting)

* **代码位置:** `framework.aspectj.RateLimiterAspect.java`
* **目标与价值:** 保护系统免受恶意的高频请求冲击，保障服务的稳定性。通过在需要保护的API方法上添加 `@RateLimiter`
  注解，可以限制该接口在单位时间内的最大访问次数。其底层通常基于令牌桶算法实现（如使用Redis+Lua或Guava的RateLimiter），当请求频率超过阈值时，会直接拒绝服务。这是API接口一道重要的安全防线。

### 3.4. 防止重复提交 (Repeat Submission Prevention)

* **代码位置:** `framework.interceptor.RepeatSubmitInterceptor.java`
* **目标与价值:** 保证关键操作的数据一致性，防止因网络延迟或用户误操作（如快速点击按钮）导致的数据重复创建。通过在表单提交等关键方法上添加
  `@RepeatSubmit` 注解，系统会在指定的时间窗口内（如3秒）拒绝内容完全相同的重复请求。其实现原理是将请求的摘要（URL+请求体）作为Key存入Redis并设置一个短暂的过期时间，后续请求若发现Key已存在则直接拦截。

### 3.5. 多数据源与读写分离 (Dynamic Data Source)

* **代码位置:** `framework.aspectj.DataSourceAspect.java`, `framework.datasource.DynamicDataSource.java`
* **目标与价值:** 提升数据库的并发处理能力和查询性能。系统通过 `@DataSource`
  注解和AOP切面，实现了动态切换数据源的功能。开发者可以简单地通过注解，将“写”操作（增、删、改）指定到主数据库（MASTER），将“读”操作（查询）指定到从数据库（SLAVE）。这种“读写分离”的架构可以将查询负载分摊到多个从库上，有效降低主库的压力，是应对高并发访问的常用数据库架构策略。
    * **实现方式：** 该控制器直接注入 `RedisTemplate`，并调用其API来与Redis交互。例如，获取缓存列表是通过
      `redisTemplate.keys("*")` 命令实现的。
    * **注意：** 这是一个相对简陋和危险的功能。在生产环境中，`keys(*)` 命令会阻塞Redis，对性能造成巨大影响。此功能更适合在开发和测试环境中使用。

---

## 第三部分：系统工具 (System Tools)

“系统工具”是为开发者提供的、旨在提升开发效率的辅助功能。

### 3.1. 表单构建

* **业务目标：**
  提供一个可视化的、拖拽式的界面，让非开发人员或前端开发者能够快速地设计和生成各类表单，而无需编写复杂的代码。

* **核心功能点：**
    * **可视化设计：** 从左侧的组件库中拖拽输入框、下拉框、日期选择器等组件到中间的画布上。
    * **属性配置：** 在右侧的属性面板中，为选中的组件配置其标签、占位符、校验规则等。
    * **预览与生成：** 实时预览表单的最终效果，并生成可用的JSON配置或前端代码。

* **技术实现逻辑：**
    * **实现模式：** 这通常是一个高度集成的第三方或自研的前端组件。
    * **前端：** 使用 `v-form-designer` 或类似的开源库。核心是拖拽功能（`vuedraggable`）和一个用于描述表单结构的JSON Schema。
    * **后端：** 后端在此模块的角色相对简单，主要是提供一个存储和读取表单设计JSON的CRUD接口。

### 3.2. 代码生成

* **业务目标：**
  将开发过程中最重复、最繁琐的CRUD（增删改查）代码编写工作自动化，极大地解放生产力。

* **核心功能点：**
    * **导入数据表：** 从当前数据库中选择一个或多个表，导入到代码生成模块进行管理。
    * **编辑生成配置：** 为每个表配置其生成代码的详细信息，如模块名、业务名、作者、实体类名，以及每个字段的Java类型、在表单中的显示类型（文本框、下拉框等）和校验规则。
    * **预览与生成：** 在生成前，可以预览将要生成的每一份代码文件（Controller, Service, Mapper, Domain, Vue, SQL,
      API.js）。确认无误后，一键生成所有代码，可以直接下载为zip包，或在特定配置下直接写入项目目录。

* **技术实现逻辑：**
    * **核心框架：** **Apache Velocity** 模板引擎。
    * **控制器：** `GenController.java`。
    * **实现流程：**
        1. **元数据查询：** `GenTableServiceImpl` 通过查询 SQL Server 的 `sys.tables`、`sys.columns` 及
           `sys.extended_properties` 获取表的结构信息及扩展属性（注释）。
        2. **配置持久化：** 用户的生成配置被保存在 `gen_table` 和 `gen_table_column` 表中。
        3. **模板渲染：** 当用户点击生成时，后端会准备一个包含所有配置信息和元数据的“上下文对象”（`Context`）。然后，它会遍历
           `resources/velocity` 目录下的所有代码模板文件（`.vm` 文件），使用Velocity引擎，用上下文对象去“渲染”这些模板，将模板中的变量（如
           `$!{className}`）替换为实际值，从而动态生成代码字符串。

### 3.3. 系统接口

* **业务目标：**
  为前后端开发者提供一个自动生成、实时更新、可交互的API文档。

* **核心功能点：**
    * **接口列表：** 按控制器分组，列出系统中所有的API接口。
    * **接口详情：** 展示每个接口的URL、请求方法、请求参数、响应模型等。
    * **在线调试：** 提供“Try it out”功能，可以直接在文档页面上填写参数并发起API请求，查看真实响应。

* **技术实现逻辑：**
    * **核心框架：** **SpringDoc** (基于 OpenAPI 3 规范，是 Swagger 的继任者)。
    * **实现方式：** 项目中引入了 `springdoc-openapi-starter-webmvc-ui` 依赖。SpringDoc会在应用启动时，自动扫描所有带有
      `@RestController` 和 `@RequestMapping` 等注解的类和方法，并根据这些信息生成一个符合OpenAPI 3规范的JSON描述文件。
    * **前端展现：** “系统接口”这个菜单页面，实际上是一个指向SpringDoc自带的UI界面的 `iframe` 或重定向。这个UI（通常在
      `/swagger-ui.html` 或 `/doc.html`）会加载上述的JSON描述文件，并将其渲染成用户友好的可交互文档。